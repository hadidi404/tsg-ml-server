name: tsg_application
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.0.0
  camera: ^0.10.5+5
  image_picker: ^1.0.4
  http: ^1.1.0
  web_socket_channel: ^2.4.0
  provider: ^6.1.1
  image: ^4.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
